service: layer-dev-libraries-s3

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  profile: mos-api-programmatic-admin
  deploymentBucket:
    name: mos-api-dev-layers

layers:
  LayerDevLibrariesS3:
    path: nodejs
    description: "AWS Lambda Layer containing S3 library from Libraries directory."